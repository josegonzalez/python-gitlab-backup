#!/usr/bin/env python
"""
Backwards-compatible wrapper script.

The recommended way to run gitlab-backup is via the installed command
(pip install gitlab-backup) or python -m gitlab_backup.

This script is kept for backwards compatibility with existing installations
that may reference this path directly.
"""

from gitlab_backup.cli import run

if __name__ == "__main__":
    run()
